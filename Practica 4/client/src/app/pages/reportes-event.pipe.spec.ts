import { ReportesEventPipe } from './reportes-event.pipe';

describe('ReportesEventPipe', () => {
  it('create an instance', () => {
    const pipe = new ReportesEventPipe();
    expect(pipe).toBeTruthy();
  });
});
